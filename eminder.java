

public class eminder {
    public static void main(String[] args) {
        int dividend = 40;
        int divisor = 30;
        int remainder = dividend % divisor; // Using modulus operator to find the remainder

        System.out.println("The remainder when " + dividend + " is divided by " + divisor + " is " + remainder + ".");
    }
}
